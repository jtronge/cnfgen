# CNFGEN

## Dependencies and installation

For optimization, you will need to install [SBVA](https://github.com/hgarrereyn/SBVA) and make sure that the `sbva` binary is accessible from your search path (`$PATH`).

Installation is through pip with a simple `pip install $repo_path`.

## Testing

Install pytest with `pip install '.[test]'`. Then run pytest.
